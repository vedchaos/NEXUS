# NEXUS v2.0 Python Dependencies
# Install: pip install -r requirements.txt

# === Core ===
chromadb>=0.4.0
requests>=2.31.0

# === Voice ===
openai-whisper>=20231117    # STT (pulls torch automatically)
pyttsx3>=2.90               # offline TTS
PyAudio>=0.2.14             # microphone input

# === Vision ===
pytesseract>=0.3.10         # OCR via Tesseract CLI
Pillow>=10.0.0              # screenshot + image ops

# === ML ===
numpy>=1.24.0
scikit-learn>=1.3.0         # classifiers, regressors, preprocessing

# === Note: torch is installed automatically by openai-whisper ===
# === Note: Tesseract OCR binary installed separately via winget ===
